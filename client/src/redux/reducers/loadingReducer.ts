const initialState = {
  newPostLoading: false,
}

// TODO: set action type, types
export const loadingReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state
  }
}

export default loadingReducer
